# GaussianFilter
GaussianFilter using several ways. first used a naive way to apply the Gaussian Filter, and dft, and then fft. 

## DFT.py
It contains 1D and 2D from scratch, and np.fft to compare output. Since it uses four nested for loop, it is O(m*n)^2.